import { FilerTodoPipe } from './filer-todo.pipe';

describe('FilerTodoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilerTodoPipe();
    expect(pipe).toBeTruthy();
  });
});
